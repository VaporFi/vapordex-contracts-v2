{
  "name": "vapordex-contracts-v2-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compile": "turbo run compile",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "clean": "turbo run clean"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@manypkg/cli": "latest",
    "prettier": "latest",
    "turbo": "latest"
  },
  "packageManager": "yarn@1.22.19"
}
